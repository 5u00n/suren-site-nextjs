# Optional: GitHub personal access token for higher API rate limits when fetching repos.
# Without it, the app uses unauthenticated requests (60 req/hr).
# Create a token at https://github.com/settings/tokens (no scopes needed for public repos).
# GITHUB_TOKEN=
